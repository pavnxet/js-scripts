{
  "name": "thinking-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "A Cloudflare Workers remote MCP server that provides reasoning tools backed by Workers AI.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0-beta.5",
    "agents": "0.20.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
